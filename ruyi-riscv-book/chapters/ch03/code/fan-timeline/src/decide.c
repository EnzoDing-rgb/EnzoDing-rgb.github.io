/*
 * decide.c — 滞回决策（学生版）
 * 只实现 fan_decide。对照答案：make sol && ./build/fan-timeline-sol
 */
#include "fan.h"

/* STUDENT TODO [D1]：按 fan.h 契约写滞回决策。
 * 就三行：超 T_HIGH 开；低于 T_LOW 关；否则保持原状态。
 * 写对后，fan-timeline 的 SUMMARY 三数应与参考一致。 */
void fan_decide(float temp_c, int *fan_on)
{
	return;   /* ← 这里实现 */
}

# EnzoDing-rgb.github.io

这是一个使用 **GitHub Pages** 部署的个人主页仓库。

## 本地预览

在仓库根目录执行：

```bash
python3 -m http.server 5173
```

然后打开 `http://localhost:5173/`。

## 部署

把 `index.html` / `styles.css` 等文件提交并推送到 GitHub 后，Pages 会自动更新。

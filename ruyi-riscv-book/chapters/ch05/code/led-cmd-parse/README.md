# led-cmd-parse · 第五章实验一

精确匹配 MQTT 载荷 `on` / `off`，并生成 status 字符串。无 Broker、无 GPIO。

```bash
make && ./build/led-cmd-parse          # 未实现 → SOME FAIL
make sol && ./build/led-cmd-parse-sol  # 参考 → ALL PASS
```
